---
/**
 * FloatingBadge - Badge flottant "0 ligne de code"
 * Suit la page et redirige vers le CTA
 */
---

<a href="#pricing" class="floating-badge" id="floating-badge">
  <span class="badge-icon">✨</span>
  <span class="badge-text">
    <span class="badge-line1">Ce site ?</span>
    <span class="badge-line2">0 ligne de code.</span>
  </span>
  <span class="badge-cta">Découvrir →</span>
</a>

<style>
  .floating-badge {
    position: fixed;
    bottom: 2rem;
    right: 1.5rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: var(--bg-card);
    border: 1px solid var(--border-medium);
    border-radius: 0.75rem;
    text-decoration: none;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    z-index: 999;
    opacity: 0;
    transform: translateY(20px);
    animation: badgeAppear 0.5s ease 2s forwards;
    transition: all 0.3s ease;
  }

  @media (max-width: 640px) {
    .floating-badge {
      bottom: 1.5rem;
    }
  }

  @keyframes badgeAppear {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .floating-badge:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 50px rgba(0, 0, 0, 0.25), 0 0 30px var(--gold-glow);
    border-color: color-mix(in srgb, var(--gold) 40%, transparent);
  }

  .badge-icon {
    font-size: 1.25rem;
    animation: iconPulse 2s ease-in-out infinite;
  }

  @keyframes iconPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .badge-text {
    display: flex;
    flex-direction: column;
    line-height: 1.2;
  }

  .badge-line1 {
    font-size: 0.625rem;
    color: var(--text-muted);
  }

  .badge-line2 {
    font-size: 0.75rem;
    font-weight: 900;
    color: var(--text-primary);
  }

  .badge-cta {
    font-size: 0.625rem;
    font-weight: 700;
    color: var(--gold);
    padding: 0.25rem 0.5rem;
    background: var(--gold-bg);
    border-radius: 0.25rem;
    transition: all 0.3s ease;
  }

  .floating-badge:hover .badge-cta {
    background: var(--gold);
    color: black;
  }

  /* Adjust for small screens */
  @media (max-width: 480px) {
    .floating-badge {
      bottom: 1.25rem;
      right: 1rem;
      padding: 0.5rem 0.75rem;
      gap: 0.5rem;
    }

    .badge-icon {
      font-size: 1rem;
    }

    .badge-line1 {
      font-size: 0.5rem;
    }

    .badge-line2 {
      font-size: 0.625rem;
    }

    .badge-cta {
      font-size: 0.5rem;
      padding: 0.125rem 0.375rem;
    }
  }
</style>
